import 'package:flutter/material.dart';

class AppColors {
  // Primary Color
  static const Color primary = Color(0xFF6A52F3);

  // Secondary Colors
  static const Color secondary = Color(0xFF4A90E2); // A complementary blue
  static const Color accent = Color(0xFFF3A952); // A soft orange accent

  // Background Colors
  static const Color background = Color(0xFFF8F9FA); // Light background
  static const Color cardBackground = Color(0xFFFFFFFF); // White card color

  // Text Colors
  static const Color textPrimary = Color(0xFF212121); // Dark text
  static const Color textSecondary = Color(0xFF757575); // Light gray text
  static const Color textAccent = Color(0xFFFFFFFF); // Light text for buttons

  // Success, Warning, Error
  static const Color success = Color(0xFF28A745); // Green for success
  static const Color warning = Color(0xFFFFC107); // Yellow for warnings
  static const Color error = Color(0xFFDC3545); // Red for errors

  // Divider and Border
  static const Color divider = Color(0xFFBDBDBD); // Light gray divider
  static const Color border = Color(0xFFE0E0E0); // Subtle border color

  // Disabled / Inactive
  static const Color disabled = Color(0xFFB0BEC5); // Disabled elements

  // Transparent
  static const Color transparent = Colors.transparent;
}
